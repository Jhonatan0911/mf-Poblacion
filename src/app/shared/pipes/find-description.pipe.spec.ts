import { FindDescriptionPipe } from './find-description.pipe';

describe('FindDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FindDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
